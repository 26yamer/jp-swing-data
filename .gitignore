# JPXの「東証上場銘柄一覧」のキャッシュ。
# このリポジトリは public なので、JPX由来のデータをコミットすると
# 再配信に当たる（JPXサイト利用条件: 「有料・無料を問わずJPXからの
# 許諾を得ている場合を除き、商用目的によるデータ収集のほか
# 如何なる用途に関わらず二次利用及び再配信はできません。」）。
# Actions では actions/cache で実行をまたいで持ち回る（fetch.yml 参照）。
cache/
__pycache__/
*.pyc
